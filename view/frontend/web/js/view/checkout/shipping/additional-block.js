define([
    'uiComponent'

], function (Component) {
    'use strict';

    return Component.extend({
        defaults: {
            template: 'Intelipost_Pickup/checkout/shipping/additional-block'
        }
    });
});
